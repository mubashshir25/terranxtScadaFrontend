apiVersion: v1
kind: Service
metadata:
  name: terranxt-scada-frontend-service
  namespace: terranxt-scada-frontend
  labels:
    app: terranxt-scada-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: terranxt-scada-frontend




